import { jsonb, pgEnum, pgTable, text, timestamp, uniqueIndex, uuid, varchar, integer } from "drizzle-orm/pg-core";
export const memberRole=pgEnum("member_role",["owner","teacher"]); export const lessonStatus=pgEnum("lesson_status",["draft","ready"]);
export const users=pgTable("users",{id:uuid("id").primaryKey().defaultRandom(),email:varchar("email",{length:320}).notNull(),name:text("name").notNull(),createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull()},t=>[uniqueIndex("users_email_unique").on(t.email)]);
export const workspaces=pgTable("workspaces",{id:uuid("id").primaryKey().defaultRandom(),name:text("name").notNull(),createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull()});
export const workspaceMembers=pgTable("workspace_members",{id:uuid("id").primaryKey().defaultRandom(),workspaceId:uuid("workspace_id").notNull().references(()=>workspaces.id),userId:uuid("user_id").notNull().references(()=>users.id),role:memberRole("role").notNull().default("teacher")},t=>[uniqueIndex("workspace_member_unique").on(t.workspaceId,t.userId)]);
export const curriculumVersions=pgTable("curriculum_versions",{id:uuid("id").primaryKey().defaultRandom(),board:varchar("board",{length:80}).notNull(),academicYear:varchar("academic_year",{length:20}).notNull(),sourceUrl:text("source_url").notNull(),sourceLabel:text("source_label").notNull(),importedAt:timestamp("imported_at",{withTimezone:true}).defaultNow().notNull()},t=>[uniqueIndex("curriculum_version_unique").on(t.board,t.academicYear)]);
export const curriculumTopics=pgTable("curriculum_topics",{id:uuid("id").primaryKey().defaultRandom(),versionId:uuid("version_id").notNull().references(()=>curriculumVersions.id),grade:varchar("grade",{length:20}).notNull(),subject:text("subject").notNull(),book:text("book").notNull(),chapterOrder:integer("chapter_order").notNull(),chapter:text("chapter").notNull(),topicOrder:integer("topic_order").notNull(),topic:text("topic").notNull(),guidance:jsonb("guidance").notNull(),provenance:text("provenance").notNull()},t=>[uniqueIndex("topic_natural_unique").on(t.versionId,t.grade,t.subject,t.book,t.chapter,t.topic)]);
export const lessons=pgTable("lessons",{id:uuid("id").primaryKey().defaultRandom(),workspaceId:uuid("workspace_id").notNull().references(()=>workspaces.id),teacherId:uuid("teacher_id").notNull().references(()=>users.id),topicId:uuid("topic_id").notNull().references(()=>curriculumTopics.id),classGroup:text("class_group").notNull(),lessonDate:timestamp("lesson_date",{withTimezone:true}).notNull(),period:varchar("period",{length:50}).notNull(),durationMinutes:integer("duration_minutes").notNull(),status:lessonStatus("status").notNull().default("draft"),content:jsonb("content").notNull(),deletedAt:timestamp("deleted_at",{withTimezone:true}),updatedAt:timestamp("updated_at",{withTimezone:true}).defaultNow().notNull()});
