# Neon pooled connection string for the dedicated Adhyaya project. Never expose this to the browser.
DATABASE_URL=
# Neon Auth endpoint for the same production branch.
NEON_AUTH_BASE_URL=
# A stable random secret (32+ characters) used to sign httpOnly session-cache cookies.
NEON_AUTH_COOKIE_SECRET=
